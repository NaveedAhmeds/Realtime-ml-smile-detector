# Realtime-ml-smile-detector
Real-time smile detection pipeline using OpenCV and Haar cascades. Captures webcam frames, runs face ROI extraction, performs smile classification within the face region, overlays labels/bounding boxes, and persistently logs smiling samples to disk for downstream ML experimentation and dataset curation.
